var name = `gyamzo`;
var address = `${name} from Nepal`;

// don't need to add '+' when we use ``

console.log(address);
console.log(address.charAt(5));
console.log(address.endsWith("y"));
console.log(address.includes("gyamzo"));
console.log(address.toUpperCase());